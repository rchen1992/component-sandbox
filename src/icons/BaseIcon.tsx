import { styled } from '../sc-utils';

export default styled.i`
    font-family: element-icons;
    font-style: normal;
    font-weight: 400;
    font-variant: normal;
    text-transform: none;
    line-height: 1;
`;
